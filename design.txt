pages:
1 home page
2 product details(flower name*, img*, type*, price*, decription)
3 shopping cart 
4 place order (date&time, products, quantity, shipping address, card, status, payment) (fake online payment / cash or check(in person))
5 customer (settings....)  (userid*, password*, firstname*, lastname, store*, address, phone*)
6 contact us(email or phone) / help desk

database:
customer: cid*(auto_generate), password*, firstname*, lastname, store*, address, phone*
store(location): sid,  lon*, lat*, address, phone
product: pid*, pname*, ptype*, price*, dec
inventory: pid*, stock*
order: cid*, pid*, quantity*, shipping addr*, card, status*
cart: cid*, pid*, quantity*

when customer sign up, must choose a default store.


database operation:
customer: curd
product: query
inventory: query, update
order: insert, search
cart: curd
store(location)